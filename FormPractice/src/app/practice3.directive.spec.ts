import { Practice3Directive } from './practice3.directive';

describe('Practice3Directive', () => {
  it('should create an instance', () => {
    const directive = new Practice3Directive();
    expect(directive).toBeTruthy();
  });
});
